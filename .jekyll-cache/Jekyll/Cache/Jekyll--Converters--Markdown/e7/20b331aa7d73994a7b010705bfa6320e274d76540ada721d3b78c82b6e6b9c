I"�	<h3 class="lead mt-5" id="en-una-función-típica-tenemos-el-nombre-de-la-función-y-unos-parámetros-después-en-la-llamada-a-la-función-se-espera-que-haya-el-mismo-número-de-argumentos-que-de-parámetros-y-si-no-es-así-dará-un-error">En una función típica tenemos el nombre de la función y unos parámetros, después en la llamada a la función se espera que haya el mismo número de argumentos que de parámetros y si no es así dará un error.</h3>

<h3 class="lead my-5" id="los-args-y-kwargs-en-python-nos-permiten-pasar-un-número-variable-de-argumentos-a-una-función">Los <code class="language-plaintext highlighter-rouge">*args</code> y <code class="language-plaintext highlighter-rouge">**kwargs</code> en Python nos permiten pasar un número variable de argumentos a una función.</h3>

<div class="alert alert-info" role="alert">
<p><i class="fas fa-sticky-note"></i> Nota:</p>
 <p>Al definir una función los valores que se reciben se denominan <b>parámetros, pero durante la llamada los valores que se envían se denominan argumentos.&lt;/p&gt;
&lt;/div&gt;

## Función con el mismo número de parámetros y argumentos.

```py
def suma(x, y, z):
    print("Suma:", x+y+z)


suma(10, 5, 2)

# Return: 17
```
## Error al enviar un número distinto de argumentos que de parámetros: 
```py
def sumar_numeros(x, y, z):
    print("Suma:", x+y+z)

    
sumar_numeros(1, 2, 3, 4, 5, 6, 7)

# TypeError: suma() takes 3 positional arguments but 7 were given
```
## Función con distinto número de parámetros y argumentos utilizando `*args`.

El simbolo asterisco `*` nos permite hacer la llamada a la función con sin importar el número de argumentos. Los argumentos se almacenan en una tupla.

El nombre del parámetro num no importa pudes utilizar args o lo que quieras, lo que hace el trabajo es el asterisco.

```py
def sumar_numeros(*num):
    suma = 0

    for n in num:
        suma = suma + n

    print(("Suma:", suma))


sumar_numeros(1, 2, 3)
# Output: ('Suma:', 6)

sumar_numeros(1, 2, 3, 4, 5, 6, 7)
# Output: ('Suma:', 28)
```

## `*kwargs` en Python

Se utiliza para trabajar con argumentos con palabras clave. Los argumentos se almacenan en un diccionario.

El nombre kwargs es convención, no es una regla puedes llamarlos como quieras.

```py
def empleado(**kwargs):
    for key, value in kwargs.items():
        print(f"{key} : {value}")


empleado(name='Marcel', role='Python Hacker', salary='50k')

# Output: 
'''
name : Marcel
role : Python Hacker
salary : 50k
'''
```
</b></p></div>
:ET